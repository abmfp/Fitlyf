name: fitlyf_app
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0 # For Poppins font

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
